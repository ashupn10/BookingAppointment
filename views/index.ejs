<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Book Appointment</title>
</head>
<body>
    <header class="main-header">
        <h1>Book Appointment for Call</h1>
    </header>
    <hr>
    <main class="main">
        <div class="main-div">
            <form action="/" method="post">
                <label for="Name">Name</label>
                <input type="text" name="Name" value="<%= edit===true?name:''%>"><br><br>
                <label for="Email">Email</label>
                <input type="email" name="Email" value="<%= edit===true?email:''%>"><br><br>
                <label for="phone">Phone</label>
                <input type="tel" name="Mobile" value="<%= edit===true?mobile:''%>"><br><br>
                <input type="submit" value="Submit">
            </form>
        </div>
        <hr>
        <div item-list-container>
            <ul class="item-list">
                <% Users.forEach(User=>{ %>
                <li class="items">
                    <span><%=User.name%></span>
                    <span><%=User.email%></span>
                    <span><%=User.mobile%></span>
                    <a href="/edit/<%=User.id%>" class="btn">Edit</a>
                    <a href="/delete/<%=User.id%>" class="btn">Delete</a>
                </li>
                <% });%>
            </ul>
        </div>
    </main>
</body>
</html>